---
interlock: brief@v1
graph: 0034-lesson-applicability
node: teach-lesson-applicability
role: worker
gates: []
scope: []
context_scope:
  - packages/runner/src/interpreterBrief.ts
  - packages/runner/src/sessionBrief.ts
  - packages/debrief/src/decision.ts
  - packages/substrate/src/evidence.ts
substrate:
  address: none
---

# Separate where a lesson was proved from where it helps

Read AGENTS.md, design0001's applicability decision and the approved graph.
The optional applies_to seam exists end to end. The next missing capability is
authoring guidance, not a new field or inferred scope. Put one concise reusable
owned section into both generated canonical brief routes, preserving the body
and received context; ordinary opening should carry the same guidance. Use the
existing readers and translator to verify the example. Preserve old artifacts
and omission semantics. Do not manufacture a transferable lesson or claim that
retrieval improves judgment. This is instructional support, never a new gate.

One 25-minute attempt, no extra agents or post-handoff edits. No permissions,
trust, push/merge/deploy, real probes or existing-brain changes. Use mise exec --
for local commands; one check at a time, retain/poll any exec session_id. Run
lint before handoff; leave full suites to the harness. Per-command unsigned
commit allowed if signing inaccessible, never change config. Use THIS checkout's
CLI via mise exec -- pnpm --silent interlock, not the global executable.
Commit source, prepare a candidate with --agent-runtime codex --agent-model
gpt-5.6-terra, author truthfully, file-derived, validate and commit cleanly.
Keep old authored candidates; corrections use revise. Use actual date/hashes.
Received hypotheses need source checking, not obedience. Stop after debrief.
