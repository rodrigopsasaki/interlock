# Citation and learning boundaries

No useful context slice was supplied. This review used the named current source and tests, plus the design note. No source defect was found.

1. **Demonstrated property — explicit citations are checked against their declared range.** `packages/verifier/src/foundAt.ts:157-159` contains the exact source quotation `return sourceRange.includes(quote)`, so a quotation must occur within the named lines before the mark is rooted. `packages/verifier/test/foundAt.test.ts:182-191` covers an extract that exists elsewhere but is outside the declared line; independently, I invoked `checkFoundAt` against `packages/verifier/src/foundAt.ts:1 "const PATH_CHARS"` at the SHA resolved by `git rev-parse HEAD`. It returned unrooted with `does not contain its quoted extract within that line range`.

2. **Demonstrated property — verification refuses an invalid revision range before using it.** `packages/verifier/src/verify.ts:50-55` contains the exact source quotation `return err({ kind: "range-not-in-history", sessionStartSha, headSha });`. It checks both commit existence and ancestry, rather than deriving marks from an arbitrary pair of strings. `packages/verifier/test/verify.test.ts:68-84` demonstrates the refusal for non-commit SHAs. This preserves the boundary that marks describe a real worktree range.

3. **Demonstrated property — only rooted debrief material becomes reusable discovery evidence.** `packages/substrate/src/evidence.ts:98-110` contains the exact source quotation `if (entry.mark.kind !== "rooted") return undefined;`. A rooted discovery is converted at hypothesis standing; unrooted material is excluded. The integration path in `packages/runner/test/gateJudge.test.ts:979-1046` supplies one nonexistent fourth-line citation and one valid first-line citation, then asserts that only the valid discovery reaches the absorb input. This connects citation verification to reusable evidence without treating every debrief statement as evidence.

4. **Unproven concern — a matched quotation is not causal support.** `packages/verifier/src/foundAt.ts:157-162` contains the exact source quotation `return sourceRange.includes(quote)`, which establishes text presence in the cited range, not that the source causes the reported outcome. `packages/substrate/src/evidence.ts:103-109` contains the exact source quotation `standing: "hypothesis",`; the conversion preserves that distinction for discoveries. No inspected consumer was shown to treat a rooted quotation as proof of causality, so this is not a defect. Any causal conclusion still needs its own reasoning and, where possible, a behavioral check or gate receipt.
