# Test-design treatment: actual exposure and report limits

Final c4894f8ade801621040753533e5b55d7167b3d0f; six harness gates cleared.
That is not acceptance of every report claim or the comparison protocol.

Runtime log `/Users/rodrigosasaki/.codex/sessions/2026/09/15/rollout-2026-09-15T18-16-38-01a0a6ed-cefe-7700-a701-9e176fd63b82.jsonl`, line 26,
contains CommandExecution stdout at 2026-09-15T21:17:27.923Z, 73,668 characters.
The command at line 24 searched `.interlock` with an exclusion relative to the
wrong prefix: `--glob '!sessions/**/brief.md'`. The raw result contains actual
non-own brief content, not merely filenames. Root and the read-only audit agree.
The audit initially missed this when using a truncated projection; its correction
is retained here rather than treating the earlier conclusion as evidence.

Exposed briefs: bootstrap verifier-hunks (251–258, 263–269, 292–298, 325–332),
debrief-schema (45–51, 57–69, 75–84, 91–97, 127–134), face-read (130–136),
ledger-gaps (72–78, 106–112), ledger (135–144), runner-command-gate (177–183),
shapes brief-shape (35–41, 116–123), and comparison review-a/review-b/test-design-a
(52–58 each). These include legacy mark semantics and operational instructions.
No completed prior report, debrief, notes, journal, receiver response or outcome
appears in this output. The equal fixture exceptions nevertheless did not preserve
the intended read boundary. Treat this pair as qualified, not clean attribution.

The worker's final note that no non-own contents were read is inaccurate. A second
disclosure records generated docs/shapes.md exposing old local-config text; the
generated reference already carried this history. No credential contents are
included in this record. Do not rewrite either raw artifact to sanitize the trial.

Independent source assessment, confirmed by root: case 1 joins three source lines
into a purported quotation; case 3 does not ensure its wrong line is in bounds;
the debrief says the received final-line fixture item was retained as a report
control, but the report cites other controls and does not show that use. The
true-quotation/false-interpretation boundary, sentinel repository scope and
multi-path scope fallback are supported. These are report limitations, not
demonstrated production defects or evidence that context caused worse work.
