<!-- interlock: comparison-assessment@v0 -->

# Test-design pair: bounded source assessment

Both nodes cleared six harness gates. Control final
133a3e497a3aabbb5e07ebca2f6b79dd45883f49; treatment final
c4894f8ade801621040753533e5b55d7167b3d0f. Read these judgments against the
unchanged source at 6db110bb56b85b5b4245965f3ea610be8e225e9c.

The control proposes three supported cases: lexical membership despite false
interpretation; filtering a decision with an unrooted hunk before reuse; and
preserving multi-path breadth through repository scope plus a gap. Its fixtures
are sufficient to distinguish those seams and its coverage claims match the
source. It omits command/out-of-band sentinel scope; under up-to-four cases with
no novelty quota this is a limitation, not a required repair. No received-item
reuse is claimed. Its failed focused command and YAML correction remain in notes.

The treatment's semantic-limit, out-of-band sentinel and multi-path cases are
supported. Its first displayed quotation joins separate source lines; its
wrong-range fixture does not ensure a second real line, and therefore could
pass by exercising out-of-bounds instead of the intended in-bounds wrong line.
The debrief quotes the received final-line hypothesis and reports a source check,
but the report does not actually retain that fixture control as claimed. This
does not establish the claimed report-level use. Preserve the report unchanged.

Read-audit findings separately qualify attribution; see test-design-b-read-audit.md.
Old and sibling brief content was returned, not merely their filenames. This is
not evidence that context caused report weakness. Both reports overlap on the
semantic limit and multi-path scope, with different additional coverage. Neither
implements tests. Do not score novelty, gate counts or shorter text as improvement.

Derivation: read-only independent source assessment followed by root inspection.
Conditions were visible for this assessment; it is not a blind quality study.
