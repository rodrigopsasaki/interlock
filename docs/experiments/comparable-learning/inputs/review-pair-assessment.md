# Review pair · independent assessment and root read

Frozen source6db110b; report X is the exact committed review-b report at9eb7131
(unchanged at final674f9ad); report Y is review-a at finalda10aa8. Assessment
omitted condition labels but report Y's own no-context statement partially
unblinds it. Root is unblinded. This is not a randomized or statistical study.

Both reports support exact-range matching, the non-semantic quotation limit,
and the integration filter for an invalid explicit discovery. Review-a adds a
correct revision-ancestry refusal property and keeps its no-defect result bounded.
Review-b accurately observes the command-form branch returning a rooted mark
without executing a command and subsequent hypothesis conversion. Its label
"defect" is stronger than the argument establishes: this is deliberate tested
legacy behavior. It could motivate a design concern, but no violated contract or
harmful downstream treatment of a hypothesis is demonstrated in the report.

Both omit the sentinel scope distinction. Under the bounded up-to-four ask this
is a limitation, not a required repair or automatic failure. Review-b's debrief
exactly quotes the received final-line fixture hypothesis and its derivation,
checks the cited test range, and ties its use to the report hunk. This witnesses
checked reuse. It does not show improved quality or prove context caused the
different command-form judgment. No repair is requested to clean up a raw
comparison result or manufacture a winning condition.

Direct test invocations described by workers are agent execution evidence, not
runner receipts for those invocations. The final harness separately ran the full
suite and all six declared gates at each final SHA. Review-a used a task-owned
temporary script for an independent wrong-range check and removed it; a first
guessed SHA failed and was corrected. Review-b first missed the package-local
test executable and then ran the whole verifier package through an unintended
argument separator before executing its intended focused case. Preserve those
observed process costs rather than omitting them or converting them to billed tokens.

The source-assisted review's new hypotheses were acknowledged only by its own
disposable receiver, then the receiver was stopped with its final snapshot.
The test-design treatment had already restored the original source database;
its actual context response body equals the review treatment's original response
byte-for-byte when JSON-serialized. Review-b findings cannot enter that baseline.

Assessment source: read-only independent aide against exact frozen blobs, then
root inspection. Root verified the reported extract bytes against the actual
repository; original aide links to temporary extracted paths were corrected.
