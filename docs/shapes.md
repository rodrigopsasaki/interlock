# Shapes reference

Generated by `interlock schema reference`; edits made directly to this file are overwritten. Regenerate with `pnpm interlock schema reference` after changing a schema under `schemas/`.

## brief

What a session is given: node, acceptance, gates, context slice, role.

### Versions

- `brief@v0`: the first version.
- `brief@v1`: added `graph` (required); added `node` (required); added `role` (required); added `gates` (required); added `scope` (required); added `substrate` (required); added `graph_base_sha` (optional); added `session` (optional).

### Fields (`brief@v1`)

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"brief@v1"` | yes |  |
| `graph` | string | yes |  |
| `node` | string | yes |  |
| `role` | string matching `^\S+$` | yes |  |
| `gates` | array of gate-declaration | yes | A gate as declared on a graph node, a graph, or a brief's front matter: an id, a kind, and an optional command and output check. A human gate carries no run. |
| `scope` | array of string | yes |  |
| `substrate` | object | yes | fields: address (string, required); handle (string, optional) |
| `graph_base_sha` | string matching `^[0-9a-fA-F]{40}$` | no |  |
| `session` | string | no |  |

### Examples

#### `brief@v0`

Source: `.interlock/sessions/0001-bootstrap/scaffold/brief.md`

````md
# Brief · node `scaffold` · graph `0001-bootstrap`

This brief is immutable once your session starts. If it is wrong, say so in the debrief; do not edit it.

## Node

- **graph:** `0001-bootstrap` (`.interlock/graphs/0001-bootstrap.yaml`)
- **node:** `scaffold`
- **depends on:** nothing
- **base SHA:** `de6e0499541bbc3999fc97ba2aef46849fa23818` on branch `graph/0001-bootstrap/scaffold`
- **worktree:** this directory. Work only here.

## Acceptance (verbatim from the graph)

> A pnpm workspace on the pinned Node with strict TypeScript, vitest, and @phyxiusjs/*
> dependencies, where `pnpm typecheck` and `pnpm test` run green on an empty package and
> `pnpm interlock debrief validate` exists as a stub that fails closed.

"Fails closed" means: the command exits non-zero with a message saying validation is not
implemented yet. It must never exit zero until a validator exists.

## Gates

Standing gates from `.interlock/config.yaml`. The harness runs these at your final commit, not you.
Run them yourself as often as you like; your runs are informational and go in the debrief.

- `typecheck` — `pnpm typecheck`
- `test` — `pnpm test`
- `debrief-valid` — `pnpm interlock debrief validate` (expected to FAIL closed for this node; the
  gate is recorded as held-by-design, since the stub is the deliverable)

## Read first

1. `AGENTS.md` — axioms, invariants, vocabulary, conventions. Binding.
2. `docs/design/0001-interlock.md` — sections "The shape", "Seams", "Two seams that cannot be
   retrofitted". You are laying the floor the `ledger` node will build on: the ledger will be a
   Phyxius journal with typed handlers, clock, and effect. Choose dependencies and layout so that
   node does not have to fight you.
3. `~/dev/private/phyxius` — the Phyxius monorepo. Read `packages/*/package.json` and each
   package's README or docs to learn which `@phyxiusjs/*` packages exist and what they are for.
   Verify each one you add is published on npm (`npm view <name> version`). Add only packages
   the ledger node will plausibly need. Record the choice and why in the debrief.

## Constraints

- **Comments: as few as possible. This is a public face.** Names, types and module boundaries
  carry the meaning. A comment is allowed only where the code genuinely cannot say it (a
  non-obvious why, an external constraint). No banner comments, no restating the code, no
  section markers, no JSDoc on things whose name and type already say it.
- Clean, readable, modularized. Small files with one purpose, named for what they are.
- Strict TypeScript. No `as`, no `!`, no `as unknown as`, no `any`. Narrow or write a real adapter.
- Node pinned by `.node-version` (24.14.0). If `node -v` disagrees, run through `mise exec --`.
  Do not change the pin.
- pnpm workspace. Match conventions in `~/dev/private/phyxius` where they apply (tsup, vitest,
  strict tsconfig). Do not add Nx or any task runner unless you can justify it in one sentence
  in the debrief; a plain pnpm workspace is preferred at this size.
- No README, no docs files, no CHANGELOG. `AGENTS.md` and the design note already exist and are
  not yours to edit.
- Do not build anything from later nodes: no ledger, no runner, no herdr adapter, no verifier.
  A package directory for the CLI stub is expected; a package for the ledger is not.
- Conventional Commits. Subject states the *why*; body justifies the choice and tradeoffs.
  Commit on this branch. **Do not push.**
- Do not create files outside this worktree. Do not use `/tmp`. No `rm -rf`.

## Deliverable

1. Commits on this branch that satisfy the acceptance.
2. `.interlock/sessions/0001-bootstrap/scaffold/debrief.yaml` in the shape below, committed as
   the final commit, with `head_sha` set to the commit before it (the last code commit).

## Debrief shape (`debrief@v0`)

This is the first debrief ever written. If the shape cannot express something you need to say,
put it under `open` and say what the shape lacks. That is a finding, not a failure.

```yaml
interlock: debrief@v0
graph: 0001-bootstrap
node: scaffold
base_sha: de6e0499541bbc3999fc97ba2aef46849fa23818
head_sha: <sha of your last code commit>
derivation:
  kind: agent
  runtime: <e.g. claude-code>
  model: <the model id you are running as, as best you know it>
discoveries:
  # Things you had to find out that this brief did not tell you.
  # Every discovery is a brief deficiency; be generous.
  - id: d1
    what: <one sentence>
    found_at: <path[:lines] | command you ran | url>
    mattered_because: <one sentence>
decisions:
  # Every choice you made. Every hunk in your diff should be explained by at least one decision.
  - id: c1
    what: <one sentence>
    rests_on: [<d-id> | brief:<section name>]
    hunks: [<path> | <path:start-end>]
    rejected: <alternatives considered, optional>
gates_run_by_agent:
  - { id: typecheck, result: pass | fail, note: <optional> }
  - { id: test, result: pass | fail }
  - { id: debrief-valid, result: fail, note: fails closed by design }
open:
  # Anything unresolved, anything the acceptance or this brief got wrong, anything the debrief
  # shape could not express.
  - <one sentence each>
```
````

#### `brief@v1`

Source: `.interlock/sessions/readme-interlock-concept/readme/brief.md`

```yaml
interlock: brief@v1
graph: readme-interlock-concept
node: readme
role: worker
gates:
  - id: typecheck
    kind: command
    run: pnpm typecheck
  - id: test
    kind: command
    run: pnpm test
  - id: debrief-valid
    kind: command
    run: pnpm interlock debrief validate {graph} {node}
  - id: readme-whitespace
    kind: command
    run: git diff 1aef03d --check
  - id: readme-assets
    kind: command
    run: >-
      node --input-type=module -e "import {readFileSync,existsSync} from 'node:fs';
      const text=readFileSync('README.md','utf8');
      const files=['docs/brand/interlock-wordmark.png','docs/brand/interlock-concept.svg','docs/brand/interlock-concept-mobile.svg','docs/brand/interlock-critical-path.svg'];
      if(files.some(file=>!existsSync(file)||!text.includes(file)))throw Error('A README asset is missing or unused');
      if(/<style|<script|style=|class=|file:\/\/|127\.0\.0\.1|\/Users\//i.test(text))throw Error('README contains local-only styling or paths');
      console.log('README assets and markup checked');"
    expect_output: README assets and markup checked
scope:
  - README.md
  - docs/brand
  - scripts/check-readme.ts
  - .interlock/sessions/readme-interlock-concept/readme
substrate:
  address: none
```

## config

No vocabulary entry for `config`.

### Versions

- `config@v0`: the first version.

### Fields (`config@v0`)

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"config@v0"` | yes |  |
| `standing_gates` | array of standing-gate | yes | A gate in the repository's standing table. Unlike a node's own gate declaration, run is required: every standing gate is a command. |
| `substrate` | object | yes | fields: address (string, required); handle (string, optional) |

### Examples

#### `config@v0`

Source: `.interlock/config.yaml`

```yaml
# The standing table for this repository.
#
# Every node whose graph runs in this repo carries these gates in addition to its own.
# A node may add gates. It may never remove one of these. This is the literal interlocking:
# the table of what must be true before a route can clear.
#
# The commands below name package scripts the `scaffold` node of the bootstrap graph had to
# create. The runner runs them for every node.

interlock: config@v0

standing_gates:
  - id: typecheck
    kind: command
    run: pnpm typecheck
  - id: test
    kind: command
    run: pnpm test
  - id: debrief-valid
    kind: command
    run: pnpm interlock debrief validate {graph} {node}
    # A session that produced no valid debrief is held here. A person working by hand in an
    # editor trips this gate on purpose; the post-hoc debrief is the escape hatch. The runner
    # substitutes {graph} and {node} when it runs a gate.

substrate:
  address: none
```

## debrief

What a session returns beside its diff: discoveries and decisions, typed.

### Versions

- `debrief@v0`: the first version.
- `debrief@v1`: added `role` (required); added `graph_base_sha` (required); added `session_start_sha` (required); removed `base_sha`.
- `debrief@v2`: retyped `discoveries`; retyped `decisions`; retyped `gates_run_by_agent`; added `drafted` (optional).

### Fields (`debrief@v2`)

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"debrief@v2"` | yes |  |
| `graph` | string | yes |  |
| `node` | string | yes |  |
| `role` | string | yes |  |
| `graph_base_sha` | string matching `^[0-9a-fA-F]{40}$` | yes |  |
| `session_start_sha` | string matching `^[0-9a-fA-F]{40}$` | yes |  |
| `head_sha` | string matching `^[0-9a-fA-F]{40}$` | yes |  |
| `derivation` | debrief-derivation | yes | Who ran the session that produced a debrief: an agent or a person. Narrower than, and distinct from, a receipt's derivation. |
| `discoveries` | array of discovery | yes | A discovery entry as debrief@v2 requires it in full: something the session found that its brief did not give it. |
| `decisions` | array of decision | yes | A decision entry as debrief@v2 requires it in full: because is required, rests_on and hunks are lists (possibly empty). |
| `gates_run_by_agent` | array of gate-run | yes | A gate run entry as debrief@v2 requires it in full: result is exactly pass or fail. |
| `open` | array of string | yes |  |
| `drafted` | object | no | fields: by (string, required); from (string, required) |

### Examples

#### `debrief@v0`

Source: `.interlock/sessions/0001-bootstrap/scaffold/debrief.yaml`

```yaml
interlock: debrief@v0
graph: 0001-bootstrap
node: scaffold
base_sha: 128b17cbba507a324feccc81d91b6305b92f4d43
head_sha: 25cdccba1110b995ae75892b4ac68c78caa0c079
derivation:
  kind: agent
  runtime: claude-code
  model: claude-sonnet-5
discoveries:
  - id: d1
    what: Node 24.14.0 runs .ts files directly with no flag, but does not remap a ".js" import specifier to a sibling ".ts" file the way tsc's NodeNext resolution expects.
    found_at: a probe run by hand -- "node run.ts" importing "./lib.js" (only lib.ts on disk) failed with ERR_MODULE_NOT_FOUND; importing "./lib.ts" directly worked.
    mattered_because: it forced a real choice between a tsup build for the CLI entrypoint or explicit .ts specifiers plus allowImportingTsExtensions; the ledger/runner packages that do get imported by others should not assume either without checking first.
  - id: d2
    what: The @phyxiusjs/journal package is documented as in-memory and ephemeral and explicitly states it does not provide durability.
    found_at: packages/core/journal/README.md in the phyxius monorepo, sections "What this really is" and "Journal does NOT help you with".
    mattered_because: the ledger node's acceptance requires "a process killed mid-write replays to the same projection"; journal alone cannot satisfy that, which is why @phyxiusjs/drain was added even though the design note never names it.
  - id: d3
    what: Journal's constructor (from @phyxiusjs/journal) requires a Clock -- JournalOptions.clock is not optional -- and journal's own README installs it paired with clock.
    found_at: packages/core/journal/src/types.ts:29-31 and packages/core/journal/README.md "Installation" section, both in the phyxius monorepo.
    mattered_because: confirms clock is load-bearing for journal, not a separate nice-to-have -- one more reason both belong in core together.
  - id: d4
    what: ControlledClock.advanceBy() takes a branded Millis, not a raw number; a plain `clock.advanceBy(10)` fails typecheck and needs the `ms()` helper.
    found_at: a real `pnpm typecheck` failure on packages/core/test/wiring.test.ts, TS2345 against packages/core/clock/src/controlled-clock.ts:222 in phyxius.
    mattered_because: exactly the class of friction the brief asked scaffold to surface now rather than leave for the ledger node.
  - id: d5
    what: pnpm 11 (resolved via mise's Node 24 corepack shim) enforces a default minimumReleaseAge supply-chain policy and silently rewrites pnpm-workspace.yaml with allowBuilds/minimumReleaseAgeExclude blocks on install; a brand-new @types/node patch (published the same day) failed the policy outright.
    found_at: running pnpm install and pnpm typecheck, both failing with ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION / ERR_PNPM_IGNORED_BUILDS until addressed.
    mattered_because: an unpinned "latest" @types/node would make every fresh clone's first install non-deterministic depending on the day it runs; pinned to an exact, already-aged patch instead, and left a static, minimal allowBuilds entry rather than the placeholder text pnpm wrote.
  - id: d6
    what: no @phyxiusjs package named "effect" exists; the design note's "typed handlers, clock, and effect" names a fourth primitive that has no npm package to install.
    found_at: running npm view @phyxiusjs/effect version, and a grep across phyxius's packages/*/README.md and package.json descriptions for "effect"/"outbox", both empty.
    mattered_because: stops the ledger node from looking for a package that was never published; "effect" is interlock's own word for the outbox (AGENTS.md I7), composed from journal (+drain), not imported.
  - id: d7
    what: a .gitignore already existed at the repo root (node_modules/, dist/, .worktrees/) before this session; an early `cat >` overwrote it wholesale instead of extending it.
    found_at: running git diff .gitignore after the first git add, showing a full-file replacement instead of a one-line addition.
    mattered_because: caught before committing and fixed to a one-line addition (coverage/); left as a discovery because the brief did not say a .gitignore already existed, and "read before you overwrite" should have been automatic.
  - id: d8
    what: pnpm forwards trailing arguments to a root script invoked as `pnpm <script-name> <args>` (no "run", no "--") -- e.g. `pnpm interlock debrief validate` runs the "interlock" script with argv ["debrief","validate"] appended.
    found_at: running pnpm interlock debrief validate by hand against the scaffolded root script, observed forwarding correctly.
    mattered_because: the entire debrief-valid gate's command shape depends on this undocumented-in-the-brief forwarding behavior actually working; verified rather than assumed.
  - id: d9
    what: Handler's defineHandler (from @phyxiusjs/handler) requires a Validator-conformant input/output schema; its own quick start uses zod, which is not a phyxius dependency itself.
    found_at: packages/components/handler/README.md "Quick start", in the phyxius monorepo.
    mattered_because: constructing a real handler costs an extra dependency (zod or equivalent) that scaffold should not pre-add speculatively; recorded so the ledger node budgets for it.
decisions:
  - id: c1
    what: Scaffolded a flat, Nx-free pnpm workspace (root package.json, pnpm-workspace.yaml, tsconfig.json, vitest.config.base.ts, and each package's vitest.config.ts) matching phyxius's strict compiler options and shared-vitest-baseline pattern.
    rests_on: ["brief:Constraints", "brief:Read first"]
    hunks: [package.json, pnpm-workspace.yaml, tsconfig.json, vitest.config.base.ts, packages/cli/vitest.config.ts, packages/core/vitest.config.ts]
    rejected: Nx, matching phyxius exactly -- rejected because at two packages it adds a task-runner layer with none of our judgment in it; plain `pnpm -r` covers typecheck and test.
  - id: c2
    what: .gitignore keeps its three pre-existing entries and gains exactly one line (coverage/) instead of being replaced.
    rests_on: ["d7"]
    hunks: [.gitignore]
  - id: c3
    what: Pinned @types/node to an exact, already-aged patch (24.13.3) in both packages and left pnpm-workspace.yaml with a static allowBuilds entry approving esbuild, rather than the placeholder pnpm generates, so a fresh install is deterministic and non-interactive.
    rests_on: ["d5"]
    hunks: [pnpm-workspace.yaml, packages/cli/package.json, packages/core/package.json, pnpm-lock.yaml]
  - id: c4
    what: cli holds the interlock CLI's only required command -- debrief validate always returns a non-zero exit with an explicit not-implemented message, and any other command shape fails closed the same way rather than being silently accepted.
    rests_on: ["brief:Acceptance", "brief:Constraints"]
    hunks: [packages/cli/package.json, packages/cli/src/main.ts, packages/cli/src/debrief/validate.ts, packages/cli/test/main.test.ts]
    rejected: a single-file CLI -- rejected in favor of splitting the pure, testable dispatcher (main.ts's run()) from the thin, untested argv/exitCode shell (bin.ts), so the fail-closed behavior is unit-tested directly rather than only through a process spawn.
  - id: c5
    what: cli's entrypoint runs directly via Node's native TypeScript execution (root's interlock script calls bin.ts with no build step), which requires allowImportingTsExtensions plus noEmit and explicit .ts specifiers on its relative imports -- the one package in the workspace that departs from the NodeNext .js-specifier convention the rest extend without note.
    rests_on: ["d1"]
    hunks: [packages/cli/tsconfig.json, packages/cli/src/bin.ts, packages/cli/src/main.ts, packages/cli/test/main.test.ts, package.json]
    rejected: a tsup build producing dist/bin.js, matching phyxius exactly -- rejected because this package has no consumers to publish a dist for yet, and a build step that must run before every `pnpm interlock ...` invocation is ceremony this stub does not earn; revisit once the CLI grows real consumers or commands.
  - id: c6
    what: core declares the four @phyxiusjs dependencies the ledger node will plausibly need (journal, clock, handler, drain), each justified individually rather than pulled in only transitively through handler.
    rests_on: ["brief:Read first", "d2", "d3", "d6", "d9"]
    hunks: [packages/core/package.json, pnpm-lock.yaml]
    rejected: depending on @phyxiusjs/handler alone and relying on its transitive re-export of journal/clock -- rejected because pnpm's non-hoisted node_modules would make journal/clock unresolvable as direct imports from core without declaring them directly (phantom-dependency risk), and because the ledger's own event log is a distinct concern from handler's internal observability journal.
  - id: c7
    what: core's src/index.ts is deliberately empty (export {}) -- no domain types, no ledger logic -- since that is the next node's acceptance, not this one's.
    rests_on: ["brief:Acceptance", "brief:Constraints"]
    hunks: [packages/core/src/index.ts, packages/core/tsconfig.json]
  - id: c8
    what: A wiring smoke test exercises journal+clock for real (construct, append, advance the controlled clock, assert sequence and wall time) and drain for real (construct against an empty compositeSink, append, stop), proving the runtime composition works rather than only that `npm install` succeeded; handler is declared but left unexercised.
    rests_on: ["d4", "d9"]
    hunks: [packages/core/test/wiring.test.ts]
gates_run_by_agent:
  - { id: typecheck, result: pass }
  - { id: test, result: pass }
  - { id: debrief-valid, result: fail, note: "fails closed by design -- exits 1 with a not-implemented message" }
open:
  - This session's base_sha (128b17c, the commit that added the brief) differs from the brief's own "base SHA" line (de6e0499..., the graph's base commit before the brief existed). Per the session instructions this is expected, not a brief error, but it means the brief's stated base SHA and this debrief's base_sha will never match for this node.
  - Whether the ledger uses @phyxiusjs/drain or Journal's own toJSON/fromJSON snapshot for crash-only durability is left open on purpose; core depends on both journal and drain so the ledger node can pick either without touching this package.json again.
  - The @phyxiusjs/handler dependency is declared but never constructed here; a real handler needs a Validator-conformant schema (handler's own quick start uses zod), which is a cost the ledger node will pay when it defines its first gate/verb handler, not one scaffold should pre-pay speculatively.
  - The debrief shape's "hunks" field has no way to mark a hunk as generated (pnpm-lock.yaml) versus hand-authored; I cited the lockfile under the decisions whose dependency changes produced it, but a verifier walking hunks literally will find 1080 lines with no single decision "explaining" its content line-by-line.
  - While drafting an early commit message I ran `pnpm interlock debrief validate` once directly (not through `mise exec --`), outside the pinned Node 24.14.0 toolchain, before catching a shell backtick-interpolation bug in that same commit and fixing it. The command is read-only (the CLI stub itself), so nothing was mutated, but it ran under the ambient node v26.0.0 / pnpm 11.20.0 for that one invocation rather than the pinned versions. Recorded for honesty, not because it changed anything in the diff.
```

#### `debrief@v1`

Source: `.interlock/sessions/0001-bootstrap/face-read/debrief.yaml`

```yaml
interlock: debrief@v1
graph: 0001-bootstrap
node: face-read
role: worker
graph_base_sha: 5778fcf69d3d2f1c3f08851c6ad90fad9e00be48
session_start_sha: 49125b495ae7ed0501e4cb5908af7fe9e2c4fd7b
head_sha: 19e9649676bd93031cb178024091c86132347b07
derivation:
  kind: agent
  runtime: claude-code
  model: claude-sonnet-5
discoveries:
  - id: d1
    what: >
      Raw Node's ESM loader does not remap a ".js" import specifier to a sibling ".ts" file across
      a package boundary either -- the same gap scaffold's own d1 named for a single file, one
      level up, at the point where one workspace package imports another. packages/ledger/src's
      own internal imports all use ".js" specifiers pointing at ".ts" files, which vitest and
      tsc's own NodeNext resolution both tolerate, but which raw Node (the interpreter
      packages/cli's bin.ts actually runs under, with no build step) does not. Nothing before
      face-read had ever imported ledger at runtime, so this had never mattered.
    found_at: >
      two probes run by hand before touching any real file: `node run.ts` importing `./lib.js`
      (only lib.ts on disk) failed with ERR_MODULE_NOT_FOUND, matching scaffold's own d1 exactly;
      a second probe (two directories, a manual node_modules symlink, one package.json "main"
      pointing at "./src/index.ts") proved that a bare package specifier resolves correctly under
      raw Node when the target package's own internal imports also use explicit ".ts" specifiers.
    mattered_because: >
      This is the one discovery this session's whole cross-package architecture rests on: without
      it, `pnpm interlock graph show/approve` would fail at the first `import ... from "ledger"`
      the moment bin.ts actually ran, no matter how correct face's or cli's own code was, and the
      fix (converting ledger's own internal imports) touches 20 files that are otherwise
      untouched by this node's own acceptance.
  - id: d2
    what: >
      TypeScript's NodeNext module resolution treats a ".js" specifier with only a sibling ".ts"
      file on disk as legal by design (the "author in .ts, a future build fills in .js"
      convention), independent of the allowImportingTsExtensions flag -- that flag only governs
      whether an explicit ".ts" specifier is itself legal to write, not whether a ".js" one
      resolves to a ".ts" file.
    found_at: >
      `pnpm --filter ledger typecheck` and `pnpm --filter ledger test` both passed clean
      immediately after converting only packages/ledger/src's own imports (not
      packages/ledger/test's, which still use ".js" specifiers) -- no compiler or test-runner
      error appeared naming the untouched test files.
    mattered_because: >
      Confirmed that fixing d1 required touching only ledger's src/ (20 files), not its test/
      (12 files) too -- a materially smaller, more mechanical diff than converting the whole
      package would have been.
  - id: d3
    what: >
      The bootstrap graph's own unweighted critical path runs scaffold -> ledger ->
      runner-command-gate -> face -> self-run (five nodes), not through verifier-hunks, even
      though verifier-hunks depends on two nodes (debrief-schema and runner-command-gate) where
      face only depends on one path through runner-command-gate. Both chains tie at length five;
      face's branch wins because runner-command-gate is listed before debrief-schema in every
      node that depends on both, and the tie-break keeps the first-encountered maximum.
    found_at: >
      hand-computed against the real .interlock/graphs/0001-bootstrap.yaml before writing
      criticalPath.test.ts's own assertion, then confirmed by running the real CLI
      (`mise exec -- pnpm interlock graph show 0001-bootstrap`) against the actual repository and
      reading its "critical path:" line.
    mattered_because: >
      criticalPath.test.ts's "matches the bootstrap graph's own five-node chain" case encodes this
      verified real answer, not an assumption about which branch should win a tie.
  - id: d4
    what: >
      Every writable location under this worktree, by construction, is nested somewhere under the
      worktree's own root -- which does carry a real .interlock/. A test fixture built under
      packages/cli/test/.runs/ therefore can never exercise findRepoRoot's "no .interlock
      directory found in this directory or any parent" branch: the walk always finds the real one
      partway up, before reaching any filesystem boundary.
    found_at: >
      a failing assertion in an early draft of graph/show.test.ts: a fixture cwd with no
      .interlock/ of its own still resolved the worktree's real graphs directory instead of
      refusing, because findRepoRoot correctly kept walking up past it.
    mattered_because: >
      Removed that one test rather than construct a fixture outside the worktree (which the
      brief's own constraints forbid) to make a false refusal pass; findRepoRoot's "not found"
      branch stays real, reachable code with no dedicated regression test, which is an honest gap
      given the constraint, not an oversight -- recorded under `open`.
  - id: d5
    what: >
      `git rev-parse HEAD` run with its cwd set to a gitignored temp fixture nested inside this
      worktree resolves the enclosing repository's real current HEAD, since git's own discovery
      walks up past a directory with no .git of its own to find the one that does.
    found_at: >
      packages/cli/test/graph/approve.test.ts's fixtures (under packages/cli/test/.runs/), which
      are plain directories with no git repository of their own, yet every approve call against
      them succeeds and records a real, valid commit SHA.
    mattered_because: >
      No dependency injection or fixture git-init was needed to make approve's commitSha field
      honest in tests; the same fact that produced d4's gap (fixtures are always nested inside a
      real repository) made this one free instead of costly.
  - id: d6
    what: >
      The "yaml" npm package's most recent stable release before this session, 2.9.0, was
      published 2026-05-11 -- about four months before this session's own date -- well outside
      any plausible pnpm minimumReleaseAge window.
    found_at: "`npm view yaml versions --json` and `npm view yaml time --json`, run before adding the dependency."
    mattered_because: >
      Pinned to this exact, already-aged version per scaffold's own d5/c3 precedent for
      @types/node; `pnpm install` afterward showed no ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION and
      left pnpm-workspace.yaml's allowBuilds untouched.
  - id: d7
    what: >
      No repo-root-finder utility exists anywhere in packages/ledger/src or packages/cli/src.
    found_at: "`grep -rn` for \"interlock\"/\"repoRoot\"/\"findRoot\"-shaped names across both packages' src trees before writing packages/face/src/root.ts, confirming the scout's own note."
    mattered_because: >
      Confirmed this was new code to write, not something to reuse, before writing it -- matching
      the discipline of reading before guessing rather than assuming the scout's absence-of-
      mention was itself proof.
  - id: d8
    what: >
      @phyxiusjs/clock's published 0.3.0 build (the version this workspace already pins) exports
      createSystemClock as a real, public function.
    found_at: "node_modules/.pnpm/@phyxiusjs+clock@0.3.0.../dist/index.d.ts's own export list, read directly after `pnpm install`, not assumed from the scout's own flagged-as-unverified note."
    mattered_because: >
      graph approve's clock (passed to createLedger) depends on this export existing; the scout
      explicitly named this as unverified pending install, so it was checked directly rather than
      carried forward as an assumption.
  - id: d9
    what: >
      The real, journal-less repository's `interlock graph show 0001-bootstrap`, before any
      approval exists, reports every one of the graph's eight nodes as ready or blocked on its own
      unmet dependencies, the five-node critical path from d3, and the graph itself as
      "not approved" -- then, after a real `interlock graph approve 0001-bootstrap --by "Rodrigo
      Sasaki" --because "approved in conversation on 2026-09-09 against 79521ddccff79088"`, the
      identical show command reports "approved" and nothing else in the position changes.
    found_at: >
      run for real in this worktree at head_sha 4a17d75cbadf3c637fd8fea9ae6ca47010a70fe2. Before:
      "graph 0001-bootstrap — not approved" with scaffold ready, every other node blocked on its
      declared dependencies, and "critical path: scaffold -> ledger -> runner-command-gate -> face
      -> self-run". The approve command's own output:
      "0001-bootstrap: approved (receipt 3a28df7be5a783b79e86a37bbf787e4d0e5e2994a68dcbbe4358429063b54af3)."
      After: "graph 0001-bootstrap — approved", every node's ready/blocked state and the critical
      path unchanged. .interlock/ledger/journal.jsonl (gitignored, real, on disk in this worktree)
      now holds exactly two lines: node-created for the pseudo node {graph: "0001-bootstrap", id:
      "0001-bootstrap"}, then gate-moved{gate: "approved", to: satisfied(receipt)} with
      commitSha 4a17d75cbadf3c637fd8fea9ae6ca47010a70fe2, derivation {kind: human, who: "Rodrigo
      Sasaki"}, and proof.because the literal string passed on the command line.
    mattered_because: >
      This is the first real receipt this journal has ever held, and it backfills the approval
      record PR #6 refers to -- the deliverable the brief explicitly asks this session to produce,
      not a dry run or a mocked example.
decisions:
  - id: c1
    what: >
      Converted every internal relative import in packages/ledger/src from a ".js" specifier to
      an explicit ".ts" one (20 files, one line each), added allowImportingTsExtensions to
      ledger's tsconfig.json, and pointed ledger's package.json "main"/"types" at "./src/index.ts".
    rests_on: ["d1", "d2", "notes:1", "notes:2", "notes:3"]
    hunks:
      [
        "packages/ledger/src/brief.ts",
        "packages/ledger/src/debrief.ts",
        "packages/ledger/src/derivation.ts",
        "packages/ledger/src/envelope.ts",
        "packages/ledger/src/event.ts",
        "packages/ledger/src/gate.ts",
        "packages/ledger/src/graph.ts",
        "packages/ledger/src/index.ts",
        "packages/ledger/src/lease.ts",
        "packages/ledger/src/ledger.ts",
        "packages/ledger/src/mandate.ts",
        "packages/ledger/src/mark.ts",
        "packages/ledger/src/note.ts",
        "packages/ledger/src/outcome.ts",
        "packages/ledger/src/projection.ts",
        "packages/ledger/src/receipt.ts",
        "packages/ledger/src/replay.ts",
        "packages/ledger/src/session.ts",
        "packages/ledger/src/sink.ts",
        "packages/ledger/src/spend.ts",
        "packages/ledger/tsconfig.json",
        "packages/ledger/package.json",
      ]
  - id: c2
    what: >
      Scaffolded packages/face as a sibling of ledger and cli: package.json ("face", depending on
      ledger via "workspace:*", @phyxiusjs/fp, and yaml pinned to 2.9.0), tsconfig.json (extends
      the root config with allowImportingTsExtensions, matching cli's own pattern), and
      vitest.config.ts (re-exports the shared baseConfig, matching ledger's and cli's own file
      verbatim).
    rests_on: ["brief:Package", "d6", "notes:4"]
    hunks: ["packages/face/package.json", "packages/face/tsconfig.json", "packages/face/vitest.config.ts"]
    produces: ["pnpm-lock.yaml"]
  - id: c3
    what: >
      Wrote packages/face/src/validate.ts as ledger's own validate.ts guard style
      (isRecord/isString/isStringArray/prop), duplicated rather than imported since ledger's
      index.ts does not export these guards.
    rests_on: ["brief:Constraints", "notes:5"]
    hunks: ["packages/face/src/validate.ts"]
  - id: c4
    what: >
      Wrote document.ts: GraphDocument/NodeDeclaration/GateDeclaration types (named apart from
      ledger's own Node/Gate, which describe a different, dependency-edge-free shape), GRAPH_SHAPE
      ("graph@v0"), loadGraphDocument (reads the file, parses it with the "yaml" package, validates
      the parsed value with hand-written guards, checks every depends_on id against the document's
      own node ids, and runs topologicalOrder to refuse a cycle), GraphRefusal's four kinds
      (missing-file/malformed-shape/unknown-dependency/cycle), and explainGraphRefusal, the one
      sentence-producing function both CLI commands call.
    rests_on: ["brief:Position rendering is pure", "brief:Errors are sentences", "notes:6", "notes:22"]
    hunks: ["packages/face/src/document.ts", "packages/face/test/document.test.ts"]
  - id: c5
    what: >
      Wrote topology.ts: topologicalOrder over NodeDeclaration[] via Kahn's algorithm, returning
      Result<readonly NodeDeclaration[], CycleRefusal>, with a cycle's refusal naming only the
      nodes actually still stuck rather than the whole graph.
    rests_on: ["brief:Node order topological by depends_on", "notes:7"]
    hunks: ["packages/face/src/topology.ts", "packages/face/test/topology.test.ts"]
  - id: c6
    what: >
      Wrote criticalPath.ts: a single forward dynamic-programming pass over an already
      topologically-sorted node list, computing the unweighted longest dependency chain by node
      count with a first-encountered-max tie-break, reconstructed by walking a predecessor map.
    rests_on: ["brief:Critical path", "d3", "notes:8", "notes:9"]
    hunks: ["packages/face/src/criticalPath.ts", "packages/face/test/criticalPath.test.ts"]
  - id: c7
    what: >
      Wrote position.ts: computePosition(document, projection, contentHash) -> Position, pure and
      synchronous, folding topological order, each node's outcome-or-ready/blocked state (ready
      when every dependency's outcome is "cleared", else blocked on the unmet ones by id), each
      declared gate's projection state or gate.pending(), the graph's own approval state via the
      standalone approvalState(gate, contentHash) function, and criticalPath's own output.
    rests_on:
      [
        "brief:Position rendering is pure",
        "brief:The graph's approval is a receipt on the graph's content",
        "brief:Approval state has three values",
        "notes:10",
        "notes:11",
      ]
    hunks: ["packages/face/src/position.ts", "packages/face/test/position.test.ts"]
  - id: c8
    what: >
      Wrote render.ts: renderPosition(position) -> text, a separate pure function from
      computePosition so every correctness test can assert against the Position value directly.
    rests_on: ["brief:Position rendering is pure", "notes:12"]
    hunks: ["packages/face/src/render.ts", "packages/face/test/render.test.ts"]
  - id: c9
    what: >
      Wrote root.ts: findRepoRoot (walks up from a start directory for the nearest ancestor
      containing .interlock/), graphFilePath, journalDirectory (.interlock/ledger under the repo
      root), and currentCommitSha (shells `git rev-parse HEAD`).
    rests_on: ["brief:Where the journal lives", "d5", "d7", "notes:13", "notes:22"]
    hunks: ["packages/face/src/root.ts"]
  - id: c10
    what: >
      Wrote index.ts: face's one public surface, one export block per module in file-tree order,
      no logic of its own, matching ledger's own index.ts shape.
    rests_on: ["brief:Constraints", "brief:Package"]
    hunks: ["packages/face/src/index.ts"]
  - id: c11
    what: >
      Wrote the stale-approval test itself (approval.test.ts): approves a real fixture graph file
      via createReceipt against real bytes on disk, edits the file by exactly one byte, and asserts
      computePosition's approval flips from "approved" to "stale" -- named so
      `--testNamePattern stale-approval` selects exactly this one test, mirroring the ledger
      session's own d1/d12 discovery about vitest's substring name matching.
    rests_on: ["brief:Approval state has three values", ".interlock/graphs/0001-bootstrap.yaml:stale-approval gate"]
    hunks: ["packages/face/test/approval.test.ts"]
  - id: c12
    what: >
      Wrote packages/cli/src/graph/show.ts: resolves the repository root and graph file path,
      loads the graph document through face's one loader, reads the journal through readReplay
      only, computes the current content hash via ledger's own receiptId, and returns
      renderPosition's text on success or a sentence (missing id, unresolvable repo root,
      malformed graph, or a journal ReplayRefusal) on failure. show.test.ts proves it opens no
      ledger for writing (createLedger and attachLedgerSink mocked and asserted uncalled, plus a
      filesystem check that no journal directory appears).
    rests_on:
      [
        "brief:show never writes",
        "docs/design/0002-face.md:D15",
        "d4",
        "notes:14",
        "notes:19",
      ]
    hunks: ["packages/cli/src/graph/show.ts", "packages/cli/test/graph/show.test.ts"]
  - id: c13
    what: >
      Wrote packages/cli/src/graph/approve.ts: refuses without --by or --because, or on a graph
      that fails the same validator show uses, before touching anything; otherwise opens
      createLedger for the call's duration, treats the graph as a pseudo node ({graph: id, id:
      id}), appends node-created only if that node is not yet in the projection, computes the
      approval receipt via createReceipt (scope: the graph file path, gate: "approved", derivation:
      human(by), proof: {because}, commitSha: the real current HEAD), moves the gate through
      proposeGateMove, appends gate-moved, and always closes the ledger -- even on a GateRefusal.
      approve.test.ts proves every refusal, the exact event sequence a first and a second approval
      each produce, and that a subsequent show reports approved, then stale after an edit.
    rests_on:
      [
        "brief:approve refuses without a because",
        "brief:The graph's approval is a receipt on the graph's content",
        "docs/design/0001-interlock.md:D20",
        "notes:15",
        "notes:16",
        "notes:17",
        "notes:18",
      ]
    hunks: ["packages/cli/src/graph/approve.ts", "packages/cli/test/graph/approve.test.ts"]
  - id: c14
    what: >
      main.ts's run() is now `async`, dispatching "graph show"/"graph approve" alongside the
      existing debrief-validate and unknown-command branches under one Promise<CommandResult>
      signature; bin.ts awaits it and routes a zero exit's message to stdout, a non-zero one to
      stderr (previously always stderr, untested against a real success message since every prior
      command always failed). main.test.ts's two existing tests now `await run(...)`.
    rests_on: ["brief:CLI run() dispatch", "notes:20", "notes:21"]
    hunks: ["packages/cli/src/main.ts", "packages/cli/src/bin.ts", "packages/cli/test/main.test.ts"]
  - id: c15
    what: >
      packages/cli/package.json gained dependencies on @phyxiusjs/clock, @phyxiusjs/fp, face, and
      ledger (the latter two via workspace:*).
    rests_on: ["brief:Package"]
    hunks: ["packages/cli/package.json"]
  - id: c16
    what: >
      .gitignore gained .interlock/ledger/ (the journal is per-machine state, not a committed
      artifact, for now -- the one decision in the brief flagged for revisiting once the runner
      exists), plus packages/face/test/.runs/ and packages/cli/test/.runs/, matching ledger's own
      test/.runs/ entry.
    rests_on: ["brief:Where the journal lives", "notes:23"]
    hunks: [".gitignore"]
  - id: c17
    what: notes.yaml is this session's own append-as-you-work record, committed across the three code commits alongside the work each entry documents, not a design choice about face's own shape.
    rests_on: ["brief:Deliverable"]
    hunks: [".interlock/sessions/0001-bootstrap/face-read/notes.yaml"]
  - id: c18
    what: >
      Fix round: removed the `as Record<string, unknown>` cast in approve.test.ts's
      journalEventKinds helper. The guard already there (typeof parsed === "object" &&
      parsed !== null && "kind" in parsed) narrows parsed enough on its own for
      String(parsed["kind"]) to compile clean under this repo's own strict tsconfig
      (noPropertyAccessFromIndexSignature included); the cast added nothing.
    rests_on: ["verifier finding 2", "notes:24"]
    hunks: ["packages/cli/test/graph/approve.test.ts"]
  - id: c19
    what: >
      Fix round: this entry and c18 close the gap a verifier found in the original filing
      (c1-c17) -- every changed file in this session's diff had a decision naming it except
      debrief.yaml itself, the one file the brief's own "decisions are a manifest of the diff"
      check was never run against before filing. Closed the same way c17 already closes it for
      notes.yaml: naming debrief.yaml as its own hunk.
    rests_on: ["brief:Deliverable", "verifier finding 1"]
    hunks: [".interlock/sessions/0001-bootstrap/face-read/debrief.yaml"]
gates_run_by_agent:
  - { id: typecheck, result: pass, invocation: "mise exec -- pnpm typecheck" }
  - { id: test, result: pass, invocation: "mise exec -- pnpm test" }
  - { id: debrief-valid, result: fail, invocation: "mise exec -- pnpm interlock debrief validate", note: "fails closed by design until the debrief-schema node lands" }
  - { id: stale-approval, result: pass, invocation: "mise exec -- pnpm --filter face test --testNamePattern stale-approval" }
open:
  - >
    Graph-as-node obstacle 1 (carried from the brief, confirmed, routed around, not fixed):
    node-created carries no gates payload (event.ts), so the graph's own declared gates can never
    be persisted on that event. Every call that needs them (proposeGateMove inside approve,
    computePosition's approval read) re-reads document.gates from the graph file itself, never
    from the projection -- exactly D14's "two sources of truth" framing, not a workaround for a
    gap.
  - >
    Graph-as-node obstacle 2 (carried from the brief, confirmed, routed around, not fixed):
    nothing at the type level stops a real node inside a graph from sharing an id with the graph
    itself, which would collide in the same nodes map (nodeKey is "<graph>::<id>", and the
    pseudo-node's key is "<graphId>::<graphId>"). Not a blocker: the bootstrap graph's own eight
    node ids never equal "0001-bootstrap". Worth a type-level fix (e.g. a branded or reserved id)
    if a future graph ever names a node after itself.
  - >
    "approval"/ApprovalState is used as a noun throughout packages/face (a Position's `approval`
    field, the approvalState function) on the strength of D20's own repeated use ("the approval is
    a mandate over the whole graph," "the approval is agnostic of what produced the plan"), but it
    is not a row in AGENTS.md's closed vocabulary table -- the same treatment, and the same gap,
    the ledger session already recorded for "disposition."
  - >
    "Document" and "Declaration" (GraphDocument, NodeDeclaration, GateDeclaration) are plain
    programming English, not AGENTS.md vocabulary or design-note nouns, used because ledger's own
    Node and Gate already name a different, narrower shape (no dependency edges, no command
    string) that both types are in scope alongside in position.ts. "Document" itself is the
    brief's own word ("a function from (graph document, projection, ...)"); "Declaration" is not.
    Neither word ever appears in rendered position text (renderPosition uses only vocabulary and
    D20-sanctioned words), so I3 is not broken by anything a person actually reads -- but both
    names are recorded here per I3's own rule that an unmatched word is a gap, never a silent
    coinage.
  - >
    loadGraphDocument rethrows any file-read error that is not ENOENT, and currentCommitSha throws
    if `git rev-parse HEAD` fails outside a git repository -- mirroring readReplay's own identical
    ENOENT-only handling in packages/ledger/src/replay.ts. Neither case is one of the five named
    sentence-error scenarios, and this repository's own worktree is always a git repository in
    every path this node's acceptance describes, so this was a deliberate parity choice with an
    existing precedent, not an oversight -- but it means an unexpected filesystem failure or a
    non-git .interlock/ repository would still surface a raw stack trace rather than a sentence.
  - >
    findRepoRoot's "no .interlock directory found" branch has no dedicated regression test: every
    writable location under this worktree's own "do not create files outside this worktree"
    constraint is, by construction, nested inside a real .interlock/-bearing repository (d4), so no
    fixture built inside the worktree can honestly exercise it. The branch is real and reachable
    code, exercised informally (by hand, before this constraint was recognized), just not covered
    by an automated test.
  - >
    Four ledger gaps this node was explicitly told not to fix, confirmed still present and routed
    around rather than patched: no Outcome kind for a voluntary, non-failure pause; reset's
    constructor takes only receipts, no because or authority; Receipt has no duration field;
    the Debrief TS type (packages/ledger/src/debrief.ts) still lacks the derivation,
    graph_base_sha, session_start_sha, and gates_run_by_agent fields this very file's on-disk YAML
    carries.
  - >
    The live demonstration this deliverable requires (d9): `interlock graph show 0001-bootstrap`
    against the real, journal-less repository, a real `interlock graph approve 0001-bootstrap
    --by "Rodrigo Sasaki" --because "approved in conversation on 2026-09-09 against
    79521ddccff79088"`, and `show` again -- run for real in this worktree, not mocked, with its
    full verbatim output recorded under discovery d9 above. This is the first real receipt this
    journal has ever held.
```

#### `debrief@v2`

Source: `.interlock/sessions/0001-bootstrap/verbs/debrief.yaml`

```yaml
interlock: debrief@v2
graph: 0001-bootstrap
node: verbs
role: worker
graph_base_sha: df184a2738ba9c652cdb760631a0590d7bc6d36a
session_start_sha: 789564dadf537cb1f1d599c88b69d90c19da1cc0
head_sha: b807e24f3a0b1608579c16a464244a2f0bfe5773
derivation:
  kind: agent
  runtime: claude-code
  model: claude-sonnet-5
discoveries:
  - id: d1
    what: >
      interlock run never checks a node's own current outcome before
      leasing it -- only that every dependency's outcome is cleared, and
      that a brief file exists. There is no "own outcome" gate on
      leasing at all today.
    found_at: |
      $ grep -n "outcome" packages/cli/src/run.ts packages/runner/src/dependencies.ts
      run.ts: no match. dependencies.ts:10 is the only outcome check in
      either file, and it reads a dependency's outcome, never the target
      node's own.
    mattered_because: >
      the brief's own words for reset ("so the runner may lease the node
      again") read as if reset lifts a mechanical block; there is none to
      lift. reset is the accountable record that a person authorized
      trying again (I10: accountability is a recorded reason, not a
      restriction), not what makes leasing possible -- a node with any
      other outcome, including no outcome at all, was always leasable by
      run's current logic. Recorded under open below; changing run.ts to
      gate on the node's own outcome is a runner decision, not this
      session's, and position@v1/run.ts are both out of scope here.
  - id: d2
    what: >
      packages/ledger/src/projection.ts's withSession silently drops any
      event for a session id the projection has not seen via a prior
      session-started event -- lease-taken included.
    found_at: |
      $ grep -n "function withSession" packages/ledger/src/projection.ts
      projection.ts:76; its body returns the projection unchanged when
      the given session id is not already a key of projection.sessions.
      Found while writing node reset's live-lease refusal test: a
      lease-taken event appended alone left the projection with zero
      sessions, so the test first fell through to the wrong refusal
      until session-started was appended before it.
    mattered_because: >
      confirms this session's live-lease check (mirroring judge.ts's own
      pattern) is reading real projection state correctly, and that any
      future direct-ledger test setup for a "live lease" scenario needs
      both events, not lease-taken alone.
  - id: d3
    what: >
      packages/runner/src/sweep.ts already writes
      outcome.cancelled(receipts, authority, because) with receipts read
      from NodeView.receipts (the node's accumulated receipt list), not
      from any prior outcome's own .receipts field.
    found_at: "packages/runner/src/sweep.ts:53-63"
    mattered_because: >
      Outcome.receipts has no documented meaning beyond its type; this is
      the one place an existing verb (the sweeper's own abandon-write)
      already answers "what receipts does a person-authored terminal
      outcome carry." node cancel and node reset both reuse it rather
      than inventing a second interpretation.
  - id: d4
    what: >
      packages/runner/src/gateJudge.ts appends receipt-written for every
      gate it runs before deciding satisfied or blocked -- a blocked
      gate has a real receipt in the node's receipt list even though
      Gate's own "blocked" variant carries no receipt field.
    found_at: "packages/runner/src/gateJudge.ts:156-192"
    mattered_because: >
      gate waive needs "the gate's latest receipt" for exactly the case
      a person waives past: a gate currently blocked on a real failure.
      Reading receipts off NodeView.receipts (filtered by gate id) reaches
      that case; reading only the current Gate value's own receipt field
      would not have.
  - id: d5
    what: >
      proposeGateMove already refuses an undeclared gate id and any move
      off an already-waived or -superseded gate, with no change needed
      to gate.ts itself.
    found_at: "packages/ledger/src/gate.ts:86-107"
    mattered_because: >
      gate waive needed no new transition rule of its own, unlike
      Outcome -- it only had to build the right gate.waived(...) value
      and pass it through the existing function, and could reuse
      graph/approve.ts's exact refusal-message shape for the
      undeclared-gate and illegal-transition cases.
decisions:
  - id: c1
    what: >
      Added proposeOutcomeMove(current, next) to
      packages/ledger/src/outcome.ts, exported it from the package, and
      covered it with new tests in outcome.test.ts.
    because: notes:1
    rests_on:
      - "brief:the ledger's own transition rules decide what is legal; an illegal move is a sentence"
      - "packages/ledger/src/gate.ts"
      - "notes:1"
    hunks:
      - "packages/ledger/src/outcome.ts:56-82"
      - "packages/ledger/src/index.ts:42-47"
      - "packages/ledger/src/index.ts:54"
      - "packages/ledger/test/outcome.test.ts:4-10"
      - "packages/ledger/test/outcome.test.ts:135-192"
  - id: c2
    what: >
      Built interlock node cancel (packages/cli/src/node/cancel.ts) and
      its fixture-backed test suite.
    because: >
      brief:interlock node cancel <graph> <node> --by <who> --because
      <why> appends outcome-set with kind cancelled, the node's current
      receipts, the authority and the because
    rests_on:
      - "packages/cli/src/graph/approve.ts"
      - "packages/ledger/src/outcome.ts"
      - "notes:2"
    hunks:
      - "packages/cli/src/node/cancel.ts:1-108"
      - "packages/cli/test/node/cancel.test.ts:1-211"
  - id: c3
    what: >
      Built interlock node reset (packages/cli/src/node/reset.ts), with
      its live-lease and nothing-to-reset refusals, and its test suite.
    because: >
      brief:sets reset ... and the node reads as ready to the runner ...
      a node with a live lease for reset (name the session and its
      expiry)
    rests_on:
      - "packages/cli/src/judge.ts"
      - "packages/ledger/src/outcome.ts"
      - "notes:3"
      - "notes:4"
    hunks:
      - "packages/cli/src/node/reset.ts:1-135"
      - "packages/cli/test/node/reset.test.ts:1-213"
  - id: c4
    what: >
      Built interlock gate waive (packages/cli/src/gate/waive.ts) and
      its test suite.
    because: >
      brief:moves the gate to waived over its latest receipt, and
      refuses when there is none
    rests_on:
      - "packages/ledger/src/gate.ts"
      - "packages/runner/src/gateCommand.ts"
      - "notes:5"
    hunks:
      - "packages/cli/src/gate/waive.ts:1-150"
      - "packages/cli/test/gate/waive.test.ts:1-292"
  - id: c5
    what: >
      Wired node cancel, node reset and gate waive into
      packages/cli/src/main.ts's group/action dispatch, and added
      routing coverage to main.test.ts.
    because: "brief:packages/cli/src/main.ts (the group and action dispatch)"
    rests_on:
      - "packages/cli/src/main.ts"
    hunks:
      - "packages/cli/src/main.ts:4"
      - "packages/cli/src/main.ts:8-9"
      - "packages/cli/src/main.ts:43-54"
      - "packages/cli/test/main.test.ts:25-51"
  - id: c6
    what: >
      Pulled graph/approve.ts's local parseFlag into
      packages/cli/src/flags.ts and pointed all four verbs at the shared
      copy.
    because: notes:6
    rests_on:
      - "packages/cli/src/graph/approve.ts"
      - "notes:6"
    hunks:
      - "packages/cli/src/flags.ts:1-7"
      - "packages/cli/src/graph/approve.ts:24"
  - id: c7
    what: >
      Added one bend-log row to docs/design/0002-face.md recording that
      the three verbs exist and that pause is not built, naming the
      missing Outcome kind and that it needs its own event version.
    because: >
      brief:Add one bend-log row in docs/design/0002-face.md ... That
      row is the only design-note edit you make
    rests_on:
      - "docs/design/0002-face.md"
      - "notes:7"
    hunks:
      - "docs/design/0002-face.md:255"
  - id: c8
    what: >
      This session's own notes.yaml, appended at every choice and
      surprise across this session's code commits.
    because: >
      brief:notes.yaml beside this brief (notes@v0), appended at every
      choice and surprise, committed
    rests_on: []
    hunks:
      - ".interlock/sessions/0001-bootstrap/verbs/notes.yaml:1-118"
gates_run_by_agent:
  - { id: typecheck, result: pass, invocation: "mise exec -- pnpm typecheck", note: "6 of 7 workspace packages carry a typecheck script; all report Done" }
  - { id: test, result: pass, invocation: "mise exec -- pnpm test", note: "472 tests across ledger, face, debrief, verifier, runner, cli (99+38+41+42+129+123)" }
  - { id: cancel-writes-outcome, result: pass, invocation: "mise exec -- pnpm --filter cli --fail-if-no-match test --testNamePattern cancel", note: "Tests 8 passed (8) -- test/node/cancel.test.ts's 7 cases plus main.test.ts's node-cancel routing case" }
  - { id: debrief-valid, result: pass, invocation: "mise exec -- pnpm interlock debrief validate 0001-bootstrap verbs", note: "run for real against this debrief and notes.yaml at head_sha, after this file was written" }
open:
  - >
    interlock run leases a node without ever reading that node's own
    current outcome (d1) -- only its dependencies' outcomes and brief
    presence. node reset therefore does not mechanically unblock
    leasing; it only records who authorized trying again and why. A
    node cancelled or held is, today, exactly as leasable as one reset
    or never run. Whether run.ts should refuse to lease a node carrying
    a non-terminal-but-unresolved outcome (held, say) without a reset in
    between is a runner decision this session did not make -- run.ts and
    position@v1 are both out of scope for verbs.
  - >
    pause has no truthful mapping (recorded in the bend log, c7): no
    Outcome kind states a voluntary, non-failure hold. It needs a
    seventh Outcome kind with its own event version -- a union-arm shape
    change, not a verb session's call to make alone.
  - >
    projection.ts's withSession stays a no-op for a session id that
    never appeared in a session-started event (d2), the same gap
    debrief-schema's own debrief already recorded against backfill.
    node reset's live-lease check reads the same projection and is
    correct given that behavior, but inherits the same blind spot: a
    node whose only session came through interlock backfill would read
    as lease-free to reset even if some other process held it, since no
    SessionView would exist to check. Not observed in practice this
    session; noted because reset is a new reader of that same shape.
```

## event

No vocabulary entry for `event`.

### Versions

- `event@v1`: the first version.
- `event@v2`: added kind `debrief-filed`; within kind `gate-moved`: retyped `to`; within kind `receipt-written`: retyped `receipt`; within kind `outcome-set`: retyped `outcome`; added kind `session-narrated`.
- `event@v3`: no field changes from the previous version.
- `event@v4`: no field changes from the previous version.

### Fields (`event@v4`)

**kind: `node-created`**

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"event@v4"` | yes |  |
| `kind` | the literal `"node-created"` | yes |  |
| `node` | node | yes | A reference to one node of one graph, embedded wherever an event names the node it happened to. |

**kind: `lease-taken|lease-renewed`**

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"event@v4"` | yes |  |
| `kind` | one of `"lease-taken"`, `"lease-renewed"` | yes |  |
| `node` | node | yes | A reference to one node of one graph, embedded wherever an event names the node it happened to. |
| `session` | string | yes |  |
| `expiry` | number | yes |  |

**kind: `lease-expired`**

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"event@v4"` | yes |  |
| `kind` | the literal `"lease-expired"` | yes |  |
| `node` | node | yes | A reference to one node of one graph, embedded wherever an event names the node it happened to. |
| `session` | string | yes |  |

**kind: `session-started`**

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"event@v4"` | yes |  |
| `kind` | the literal `"session-started"` | yes |  |
| `session` | session | yes | The ledger's own session reference: an id and the node it works. |
| `brief` | session-brief | yes | The ledger's own reduced view of a brief, embedded in a session-started event: gates and scope as plain id lists, not the richer brief@v1 front matter. |
| `graphBaseSha` | string | no |  |

**kind: `note-appended`**

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"event@v4"` | yes |  |
| `kind` | the literal `"note-appended"` | yes |  |
| `session` | string | yes |  |
| `note` | note | yes | A typed entry a session appends while it works: a choice with its because, or a surprise with expected and observed. |

**kind: `debrief-filed`**

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"event@v4"` | yes |  |
| `kind` | the literal `"debrief-filed"` | yes |  |
| `session` | string | yes |  |
| `debrief` | debrief-event | yes | The ledger's own Debrief, embedded verbatim in a debrief-filed event. Field names are the in-memory camelCase names, not the snake_case keys a debrief.yaml file carries on disk; only the full, current shape is ever embedded, since event@v1 refuses debrief-filed outright. |

**kind: `gate-moved`**

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"event@v4"` | yes |  |
| `kind` | the literal `"gate-moved"` | yes |  |
| `node` | node | yes | A reference to one node of one graph, embedded wherever an event names the node it happened to. |
| `gate` | string | yes |  |
| `to` | gate | yes | A gate's evidence lifecycle: pending, satisfied, blocked, waived, or superseded. |

**kind: `receipt-written`**

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"event@v4"` | yes |  |
| `kind` | the literal `"receipt-written"` | yes |  |
| `node` | node | yes | A reference to one node of one graph, embedded wherever an event names the node it happened to. |
| `receipt` | receipt | yes | A fact the harness observed. Content-addressed by SHA and gate. Carries its spend and its derivation. |

**kind: `outcome-set`**

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"event@v4"` | yes |  |
| `kind` | the literal `"outcome-set"` | yes |  |
| `node` | node | yes | A reference to one node of one graph, embedded wherever an event names the node it happened to. |
| `outcome` | outcome | yes | How a node ended: cleared, held, reset, failed, cancelled, or superseded. Always with its receipts. |

**kind: `outbox-intent-recorded`**

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"event@v4"` | yes |  |
| `kind` | the literal `"outbox-intent-recorded"` | yes |  |
| `node` | node | yes | A reference to one node of one graph, embedded wherever an event names the node it happened to. |
| `id` | string | yes |  |
| `intent` | string | yes |  |

**kind: `session-narrated`**

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"event@v4"` | yes |  |
| `kind` | the literal `"session-narrated"` | yes |  |
| `session` | string | yes |  |
| `at` | number | yes |  |
| `line` | string | yes |  |

### Examples

#### `event@v1`

Source: `packages/ledger/test/fixtures/journal-v1-approved.jsonl`

```json
{"interlock":"event@v1","kind":"node-created","node":{"graph":"0001-bootstrap","id":"0001-bootstrap"}}
```

#### `event@v2`

Source: `packages/ledger/test/fixtures/journal-v1-v2-2026-09-10.jsonl`

```json
{"interlock":"event@v2","kind":"gate-moved","node":{"graph":"0001-bootstrap","id":"0001-bootstrap"},"gate":"approved","to":{"kind":"satisfied","receipt":{"id":"4978e9f0f3c1f5ec5c41e2654cec9766f8ea2465be09f7ba9414318dec9816df","gate":"approved","commitSha":"1833f7950a28cf17ea02790cf7b13e70471ee323","spend":{"kind":"none"},"duration":{"kind":"unknown"},"derivation":{"kind":"human","who":"Rodrigo Sasaki"},"proof":{"because":"re-approving the unchanged plan under location-independent receipt identity"}}}}
```

#### `event@v3`

Source: `packages/ledger/test/fixtures/journal-v3-v4-2026-09-11.jsonl`

```json
{"interlock":"event@v3","kind":"session-started","session":{"id":"052f6500-4932-4fd9-bfba-0e646e60a2e3","node":{"graph":"0001-bootstrap","id":"verifier-hunks"}},"brief":{"graph":"0001-bootstrap","node":"verifier-hunks","role":"worker","acceptance":"Deterministic marks over a debrief against its diff at the debriefed SHA: every decision cites a hunk that exists; every discovery cites a location that exists with the quoted content; every hunk has a decision, or it is marked unexplained; every term is in one of the two vocabularies, or it is a gap. Marks never block. Every mark carries its derivation.\n","gates":["typecheck","test","debrief-valid","inverse-check"],"scope":[".gitignore",".interlock/config.yaml",".interlock/graphs/0001-bootstrap.yaml",".interlock/graphs/0002-shapes.yaml",".interlock/local.example.yaml",".interlock/sessions/0001-bootstrap/debrief-schema/brief.md",".interlock/sessions/0001-bootstrap/debrief-schema/debrief.yaml",".interlock/sessions/0001-bootstrap/debrief-schema/notes.yaml",".interlock/sessions/0001-bootstrap/face-read/brief.md",".interlock/sessions/0001-bootstrap/face-read/debrief.yaml",".interlock/sessions/0001-bootstrap/face-read/notes.yaml",".interlock/sessions/0001-bootstrap/ledger-gaps/brief.md",".interlock/sessions/0001-bootstrap/ledger-gaps/debrief.yaml",".interlock/sessions/0001-bootstrap/ledger-gaps/notes.yaml",".interlock/sessions/0001-bootstrap/ledger/brief.md",".interlock/sessions/0001-bootstrap/ledger/debrief.yaml",".interlock/sessions/0001-bootstrap/ledger/notes.yaml",".interlock/sessions/0001-bootstrap/runner-command-gate/brief.md",".interlock/sessions/0001-bootstrap/runner-command-gate/debrief.yaml",".interlock/sessions/0001-bootstrap/runner-command-gate/notes.yaml",".interlock/sessions/0001-bootstrap/scaffold/brief.md",".interlock/sessions/0001-bootstrap/scaffold/debrief.yaml",".interlock/sessions/0001-bootstrap/verifier-hunks/brief.md",".interlock/sessions/0002-shapes/brief-shape/brief.md",".interlock/sessions/0002-shapes/brief-shape/debrief.yaml",".interlock/sessions/0002-shapes/brief-shape/notes.yaml",".node-version","AGENTS.md","LICENSE","NOTICE","README.md","docs/design/0001-interlock.md","docs/design/0002-face.md","docs/design/0003-substrate.md","package.json","packages/cli/package.json","packages/cli/src/backfill.ts","packages/cli/src/bin.ts","packages/cli/src/brief/validate.ts","packages/cli/src/debrief/validate.ts","packages/cli/src/graph/approve.ts","packages/cli/src/graph/show.ts","packages/cli/src/main.ts","packages/cli/src/run.ts","packages/cli/src/sweep.ts","packages/cli/test/backfill.test.ts","packages/cli/test/brief/validate.test.ts","packages/cli/test/debrief/validate.test.ts","packages/cli/test/graph/approve.test.ts","packages/cli/test/graph/gitFixture.ts","packages/cli/test/graph/show.test.ts","packages/cli/test/main.test.ts","packages/cli/test/run.test.ts","packages/cli/test/sweep.test.ts","packages/cli/tsconfig.json","packages/cli/vitest.config.ts","packages/debrief/package.json","packages/debrief/src/brief.ts","packages/debrief/src/briefGate.ts","packages/debrief/src/briefScopePath.ts","packages/debrief/src/briefSubstrate.ts","packages/debrief/src/debrief.ts","packages/debrief/src/decision.ts","packages/debrief/src/discovery.ts","packages/debrief/src/gateRun.ts","packages/debrief/src/index.ts","packages/debrief/src/notes.ts","packages/debrief/src/paths.ts","packages/debrief/src/role.ts","packages/debrief/src/sha.ts","packages/debrief/src/slice.ts","packages/debrief/src/validate.ts","packages/debrief/test/brief.test.ts","packages/debrief/test/debrief.test.ts","packages/debrief/test/fixtures/brief-v1.md","packages/debrief/test/notes.test.ts","packages/debrief/test/slice.test.ts","packages/debrief/tsconfig.json","packages/debrief/vitest.config.ts","packages/face/package.json","packages/face/src/criticalPath.ts","packages/face/src/document.ts","packages/face/src/index.ts","packages/face/src/position.ts","packages/face/src/render.ts","packages/face/src/root.ts","packages/face/src/topology.ts","packages/face/src/validate.ts","packages/face/test/approval.test.ts","packages/face/test/criticalPath.test.ts","packages/face/test/document.test.ts","packages/face/test/position.test.ts","packages/face/test/render.test.ts","packages/face/test/root.test.ts","packages/face/test/topology.test.ts","packages/face/tsconfig.json","packages/face/vitest.config.ts","packages/ledger/package.json","packages/ledger/src/brief.ts","packages/ledger/src/debrief.ts","packages/ledger/src/derivation.ts","packages/ledger/src/disposition.ts","packages/ledger/src/envelope.ts","packages/ledger/src/event.ts","packages/ledger/src/expectOutput.ts","packages/ledger/src/gate.ts","packages/ledger/src/graph.ts","packages/ledger/src/index.ts","packages/ledger/src/lease.ts","packages/ledger/src/ledger.ts","packages/ledger/src/mandate.ts","packages/ledger/src/mark.ts","packages/ledger/src/note.ts","packages/ledger/src/outcome.ts","packages/ledger/src/projection.ts","packages/ledger/src/receipt.ts","packages/ledger/src/replay.ts","packages/ledger/src/session.ts","packages/ledger/src/sink.ts","packages/ledger/src/spend.ts","packages/ledger/src/upcast/v1.ts","packages/ledger/src/upcast/v2.ts","packages/ledger/src/validate.ts","packages/ledger/test/derivation.test.ts","packages/ledger/test/expectOutput.test.ts","packages/ledger/test/fixtures/journal-v1-approved.jsonl","packages/ledger/test/fixtures/journal-v1-v2-2026-09-10.jsonl","packages/ledger/test/gate.test.ts","packages/ledger/test/lease.test.ts","packages/ledger/test/mandate.test.ts","packages/ledger/test/note.test.ts","packages/ledger/test/outcome.test.ts","packages/ledger/test/projection.test.ts","packages/ledger/test/receipt.test.ts","packages/ledger/test/replay.test.ts","packages/ledger/test/spend.test.ts","packages/ledger/test/unrepresentable.test.ts","packages/ledger/test/wiring.test.ts","packages/ledger/tsconfig.json","packages/ledger/vitest.config.ts","packages/runner/package.json","packages/runner/src/backfill.ts","packages/runner/src/briefRewrite.ts","packages/runner/src/dependencies.ts","packages/runner/src/gateCommand.ts","packages/runner/src/gateJudge.ts","packages/runner/src/herdr/adapter.ts","packages/runner/src/index.ts","packages/runner/src/lease.ts","packages/runner/src/localConfig.ts","packages/runner/src/openingPrompt.ts","packages/runner/src/runtime.ts","packages/runner/src/scope.ts","packages/runner/src/sessionBrief.ts","packages/runner/src/sessionScreen.ts","packages/runner/src/sessionWait.ts","packages/runner/src/standingGates.ts","packages/runner/src/startupAnswers.ts","packages/runner/src/sweep.ts","packages/runner/src/tmux/adapter.ts","packages/runner/src/validate.ts","packages/runner/src/worktree.ts","packages/runner/src/worktreeSetup.ts","packages/runner/test/adapterBoundary.test.ts","packages/runner/test/backfill.test.ts","packages/runner/test/briefRewrite.test.ts","packages/runner/test/fixtures/herdr-socket-schema.json","packages/runner/test/gateCommand.test.ts","packages/runner/test/gateJudge.test.ts","packages/runner/test/herdr/adapter.test.ts","packages/runner/test/herdr/fakeServer.ts","packages/runner/test/lease.test.ts","packages/runner/test/liveSmoke.test.ts","packages/runner/test/localConfig.test.ts","packages/runner/test/openingPrompt.test.ts","packages/runner/test/sessionBrief.test.ts","packages/runner/test/sessionScreen.test.ts","packages/runner/test/sessionWait.test.ts","packages/runner/test/standingGates.test.ts","packages/runner/test/support/gitFixture.ts","packages/runner/test/support/memoryLedger.ts","packages/runner/test/sweep.test.ts","packages/runner/test/tmux/adapter.test.ts","packages/runner/test/worktree.test.ts","packages/runner/test/worktreeSetup.test.ts","packages/runner/tsconfig.json","packages/runner/vitest.config.ts","pnpm-lock.yaml","pnpm-workspace.yaml","tsconfig.json","vitest.config.base.ts"]},"graphBaseSha":"043c78a0bea483de7d212031255b771d493a7ab5"}
```

#### `event@v4`

_No example of `event@v4` was found in the corpus._

## graph

The directed acyclic graph of nodes produced from one ask.

### Versions

- `graph@v0`: the first version.

### Fields (`graph@v0`)

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"graph@v0"` | yes |  |
| `id` | string | yes |  |
| `gates` | array of gate-declaration | no | A gate as declared on a graph node, a graph, or a brief's front matter: an id, a kind, and an optional command and output check. A human gate carries no run. |
| `nodes` | array of object | yes | fields: id (string, required); acceptance (string, optional); depends_on (array of string, optional); gates (array of gate-declaration, optional) |

### Examples

#### `graph@v0`

Source: `.interlock/graphs/readme-interlock-concept.yaml`

```yaml
interlock: graph@v0
id: readme-interlock-concept

ask: >
  Turn the approved local presentation into a GitHub-native README on a separate branch,
  preserving the selected keystone identity and explaining interlocks, proof, plan control,
  critical-path visibility, and the intended evolution from human judgment to bounded automation.

derivation:
  kind: agent
  runtime: codex
  model: unknown

read: >
  One documentation node. Keep prose in Markdown, embed the existing wordmark and diagrams
  as repository assets, refresh implementation claims against this branch, and open a draft PR.
  Do not merge, change runtime behavior, or disturb the active development checkout.

gates:
  - id: approved
    kind: human

nodes:
  - id: readme
    acceptance: >
      README.md presents the interlock concept using GitHub-supported Markdown and HTML.
      The selected blue open loop and gold-completed loop are reused in the checked-in
      wordmark and explanatory diagrams. Prose remains searchable and accessible; unsupported
      CSS layouts become native tables, lists, or quotations. Relative image and documentation
      links resolve. Current capabilities are distinguished from planned work, including the
      newly implemented cancel, reset, and gate-waiver commands. No verbatim session history,
      dossier excerpts, private filesystem paths, or celebratory metrics appear in the public
      presentation. GitHub's rendered README and artwork are visually inspected. The branch
      is pushed and a draft PR is opened for human review; no merge is performed.
    depends_on: []
    gates:
      - id: readme-whitespace
        kind: command
        run: git diff 1aef03d --check
      - id: readme-assets
        kind: command
        run: >-
          node --input-type=module -e "import {readFileSync,existsSync} from 'node:fs';
          const text=readFileSync('README.md','utf8');
          const files=['docs/brand/interlock-wordmark.png','docs/brand/interlock-concept.svg','docs/brand/interlock-concept-mobile.svg','docs/brand/interlock-critical-path.svg'];
          if(files.some(file=>!existsSync(file)||!text.includes(file)))throw Error('A README asset is missing or unused');
          if(/<style|<script|style=|class=|file:\/\/|127\.0\.0\.1|\/Users\//i.test(text))throw Error('README contains local-only styling or paths');
          console.log('README assets and markup checked');"
        expect_output: README assets and markup checked
```

## item

No vocabulary entry for `item`.

### Versions

- `item@v1`: the first version.

### Fields (`item@v1`)

| field | type | required | description |
| --- | --- | --- | --- |
| `kind` | one of `"convention"`, `"discipline"`, `"decision"`, `"risk"`, `"value"`, `"tension"`, `"absence"` | yes |  |
| `statement` | string | yes |  |
| `because` | string | no |  |
| `scope` | one of `repository`, `path`, `organisation` | no | `repository`: no fields beyond `kind`; `path`: path; `organisation`: no fields beyond `kind` |
| `standing` | one of `"ratified"`, `"professed"`, `"observed"`, `"hypothesis"` | no |  |
| `derivation` | string | yes |  |

### Examples

#### `item@v1`

_No example of `item@v1` was found in the corpus._

## local

No vocabulary entry for `local`.

### Versions

- `local@v0`: the first version.

### Fields (`local@v0`)

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"local@v0"` | yes |  |
| `runtime` | object | yes | fields: kind (string, required); args (array of string, optional); startup_answers (array of startup-answer, optional); startup_timeout_ms (number, optional); prompt_taken_timeout_ms (number, optional) |
| `worktree_root` | string | yes |  |
| `worktree_setup` | array of string | no |  |
| `lease_ms` | number | yes |  |
| `run_timeout_ms` | number | yes |  |
| `answer_grace_ms` | number | no |  |
| `substrate` | object | yes | fields: address (string, required); handle (string, optional) |

### Examples

#### `local@v0`

Source: `.interlock/local.example.yaml`

```yaml
# Per-machine choices for the runner. Copy this file to .interlock/local.yaml (gitignored) and
# fill it in; local.yaml is never committed, so these values never leave this machine.

interlock: local@v0

# The agent CLI the runner starts inside a herdr pane, and its extra arguments.
runtime:
  kind: claude
  args: []
  # Optional. Each entry names the text the runtime shows when it needs an answer before it
  # is ready, and the keys to send it. Entries are tried in order, at most once each.
  # startup_answers:
  #   - matches: "the text the runtime shows when it needs an answer before it is ready"
  #     keys: [Down, Enter]
  # Optional. How long the runner waits for the runtime to become ready before sending the
  # opening prompt. Defaults to 60000.
  # startup_timeout_ms: 60000
  # Optional. How long the runner waits, after sending the opening prompt, for the agent to
  # move off idle before the long wait judges it. Defaults to 20000.
  # prompt_taken_timeout_ms: 20000

# Where the runner creates a node's git worktree, relative to the repository root.
worktree_root: .worktrees

# Optional. Commands run once in a node's worktree, in order, after it is created or reset and
# before its pane opens — whatever this repository needs before an agent or a gate can run in
# it. Each runs through the same pinned-toolchain invocation the gates use. Defaults to none.
# worktree_setup:
#   - "<your setup command>"

# How long a lease lasts before a sweep may call it abandoned. The runner renews it by
# heartbeat at a third of this length while a session is running.
lease_ms: 900000

# The wall timeout the runner waits for the agent to go idle, blocked or done.
run_timeout_ms: 3600000

# Optional. How long an idle or done stays open for the agent to resume after a person has answered a blocked turn, before it is judged settled. Defaults to 300000.
# answer_grace_ms: 300000

# The substrate address. Unused by this node; present so the shape is one.
substrate:
  address: none
```

## notes

No vocabulary entry for `notes`.

### Versions

- `notes@v0`: the first version.

### Fields (`notes@v0`)

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"notes@v0"` | yes |  |
| `node` | string | yes |  |
| `entries` | array of one of `choice`, `surprise` | yes | `choice`: at, chose, because, rejected (optional); `surprise`: at, expected, observed |

### Examples

#### `notes@v0`

Source: `.interlock/sessions/readme-interlock-concept/readme/notes.yaml`

```yaml
interlock: notes@v0
graph: readme-interlock-concept
node: readme
entries:
  - kind: choice
    at: "2026-09-11T13:24:03-03:00"
    chose: Keep the presentation in Markdown with separate image assets.
    because: >
      GitHub removes custom page styling. Native headings, tables, quotations, and lists keep
      explanations readable, searchable, and editable while the artwork carries the identity.
  - kind: surprise
    at: "2026-09-11T13:24:03-03:00"
    expected: The proposed quoted inline asset check could run as a command gate.
    observed: >
      packages/runner/src/gateJudge.ts splits the command on whitespace without shell parsing.
      The quoted script is therefore split into unrelated arguments. A standalone check file
      is needed. The original approved graph and brief are retained; a corrected invocation
      requires a separately recorded approval.
  - kind: choice
    at: "2026-09-11T13:24:03-03:00"
    chose: Refresh the status claims to include cancel, reset, and gate waive.
    because: >
      Those commands are present at this branch's base, although the local presentation still
      listed human-control commands as work in progress. Rich position and assisted planning
      remain separate, unfinished capabilities.
  - kind: choice
    at: "2026-09-11"
    chose: Correct the notes envelope to the existing notes@v0 reader before ingestion.
    because: >
      The first saved draft used notes/what instead of entries/chose and omitted at. Its
      contents are preserved here and in git; the first three timestamps identify their
      original recording commit rather than claiming separate observation times.
  - kind: choice
    at: "2026-09-11"
    chose: Push codex/readme-interlock-concept and create one draft PR targeting main.
    because: >
      The user explicitly authorized publishing this branch for GitHub review. The stable
      identity is rodrigopsasaki/interlock plus this head branch; query for an existing PR
      before creating one. Keep it draft, report unfinished gate approval honestly, and do
      not merge. Inspect the rendered README on the branch before declaring visual QA done.
```

## position

The current read of a graph, composed above the stream.

### Versions

- `position@v1`: the first version.

### Fields (`position@v1`)

| field | type | required | description |
| --- | --- | --- | --- |
| `interlock` | the literal `"position@v1"` | yes |  |
| `graph` | string | yes |  |
| `approval` | one of `"approved"`, `"stale"`, `"not-approved"` | yes |  |
| `nodes` | array of positionNode | yes | fields: id (string, required); dependsOn (array of string, required); state (nodeState, required); gates (array of positionGate, required); attempts (array of positionAttempt, required); float (float, required) |
| `criticalPath` | array of string | yes |  |

### Examples

#### `position@v1`

_No example of `position@v1` was found in the corpus._
