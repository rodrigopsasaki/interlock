{
  "name": "interlock",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.14.0"
  },
  "scripts": {
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "interlock": "node packages/cli/src/bin.ts",
    "lint": "biome check .",
    "format": "biome format --write .",
    "lint:fix": "biome check --write .",
    "check:comments": "node scripts/noComments.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.13",
    "typescript": "^5.7.2"
  }
}
