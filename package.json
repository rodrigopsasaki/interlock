{
  "name": "interlock",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.14.0"
  },
  "scripts": {
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "interlock": "node packages/cli/src/bin.ts",
    "lint": "biome check packages vitest.config.base.ts vitest.setup.ts scripts",
    "format": "biome format --write packages vitest.config.base.ts vitest.setup.ts scripts",
    "lint:fix": "biome check --write packages vitest.config.base.ts vitest.setup.ts scripts",
    "check:comments": "node scripts/noComments.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.13",
    "typescript": "^5.7.2"
  }
}
