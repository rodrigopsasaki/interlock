{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/rodrigopsasaki/interlock/schemas/parts/session-runtime.json",
  "title": "session-runtime",
  "description": "What a session recorded about the runtime it ran on: a declared name, kind and optional model, or the literal \"unknown\" reading of every session recorded before event@v6.",
  "oneOf": [
    { "const": "unknown" },
    {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "kind": { "type": "string" },
        "model": { "type": "string" }
      },
      "required": ["name", "kind"],
      "additionalProperties": false
    }
  ]
}
