{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/rodrigopsasaki/interlock/schemas/substrate@v1/propose.request.json",
  "title": "propose.request@substrate@v1",
  "description": "What propose takes when a session states a standing rule: the rule in the team's words and why.",
  "type": "object",
  "properties": {
    "rule": { "type": "string" },
    "because": { "type": "string" }
  },
  "required": ["rule", "because"],
  "additionalProperties": false
}
